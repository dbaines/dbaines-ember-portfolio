export default Ember.View.extend({
  didInsertElement: function(){
    // this._super();
    // Ember.run.scheduleOnce('afterRender', this, this.afterRenderEvent);
    // $(".fotorama").not(".fotorama_init").fotorama().addClass("fotorama_init");
  }
});